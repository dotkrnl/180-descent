---
layout: layouts/base.njk
---
<article class="page wrap wide">
  <header class="page-hero">
    <p class="eyebrow">{{ eyebrow or book.subtitle }}</p>
    <h1>{{ title }}</h1>
    {% if subtitle %}<p class="sub">{{ subtitle }}</p>{% endif %}
  </header>
  {{ content | safe }}
</article>

