<!doctype html>
<html lang="{{ book.language }}" data-theme="light">
  <head>
    <meta charset="utf-8">
    <title>{{ title }} · {{ book.title }}</title>
    <link rel="stylesheet" href="/assets/css/book.css">
  </head>
  <body class="print-document">
    {{ content | safe }}
  </body>
</html>

