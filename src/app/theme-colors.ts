// Keep in sync with the theme palette in
// src/assets/scss/abstracts/_tokens.scss: light is --bone, dark is --paper.
export const SITE_THEME_COLORS = {
  light: "#f5f1e9",
  dark: "#061519"
} as const;
