---
interface Props {
  class?: string;
  as?: "span" | "em" | "u";
}

const { as = "span", class: customClass }: Props = Astro.props;
const Tag = as;
const className = ["term", customClass].filter(Boolean).join(" ");
---
<Tag class={className}><slot /></Tag>
