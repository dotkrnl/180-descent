---
interface Props {
  class?: string;
  id?: string;
  [key: string]: unknown;
}

const { class: passedClass, id, ...attrs } = Astro.props;
const className = ["panel", passedClass].filter(Boolean).join(" ");
---
<div id={id} class={className} {...attrs}>
  <slot />
</div>
