---
interface Props {
  class?: string;
  id?: string;
}

const className = ["formula", Astro.props.class].filter(Boolean).join(" ");
---
<div id={Astro.props.id} class={className}>
  <slot />
</div>
