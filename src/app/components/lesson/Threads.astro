---
interface Props {
  class?: string;
  label?: string;
}

const { label } = Astro.props;
const className = ["threads", Astro.props.class].filter(Boolean).join(" ");
---
<p class={className}>{label && <><strong class="threads-label">{label}</strong> </>}<slot /></p>
