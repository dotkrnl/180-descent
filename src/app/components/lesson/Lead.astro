---
interface Props {
  class?: string;
  drop?: string;
}

const className = ["lede", Astro.props.drop && "dropcap", Astro.props.class].filter(Boolean).join(" ");
---
<p class={className}>{Astro.props.drop}<slot /></p>
