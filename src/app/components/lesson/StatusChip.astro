---
import { renderStatusChip } from "@lib/render/lesson-components";

interface Props {
  status: "ok" | "hint" | "bad";
  label: string;
  printLabel?: string;
}

const html = renderStatusChip(Astro.props);
---
<Fragment set:html={html} />
